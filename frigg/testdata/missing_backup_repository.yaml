server:
  host: localhost
  port: 8080

loki:
  endpoint: 'http://loki.example.com'

grafana:
  endpoint: 'http://example.com'

prune:
  interval: '10m'
  period: '720h'
  labels:
    app: 'grafana'

backup:
  github: {}
