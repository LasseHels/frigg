log:
  level: 'DEBUG'

loki:
  endpoint: ${LOKI_ENDPOINT}

grafana:
  endpoint: ${GRAFANA_ENDPOINT}

prune:
  dry: false
  interval: '1s'
  period: '10m'
  ignored_users:
    - 'sa-1-john-doe'
  labels:
    app: 'grafana'
    env: 'integration-test'
  lower_threshold: 2
