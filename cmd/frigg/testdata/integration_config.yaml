loki:
  endpoint: ${LOKI_ENDPOINT}

grafana:
  endpoint: ${GRAFANA_ENDPOINT}

prune:
  dry: false
  interval: '24h'
  period: '10m'
  ignored_users:
    - 'admin'
  labels:
    app: 'grafana'
    env: 'integration-test'
