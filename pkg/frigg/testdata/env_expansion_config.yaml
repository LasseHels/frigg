server:
  host: ${FRIGG_HOST}
  port: 1111
loki:
  endpoint: 'http://loki.example.com'
grafana:
  endpoint: 'http://example.com'

prune:
  dry: true
  namespaces:
    - default
  interval: '10m'
  period: '720h'
  labels:
    app: 'grafana'
